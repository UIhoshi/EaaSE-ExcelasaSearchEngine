# 功能需求规格说明书 (REQUIREMENTS)

## 1. 搜索逻辑
- 仅允许严格子串匹配，不做模糊检索
- 输入过程中提供候选词
- 点击候选词或按 `Enter` 触发搜索

## 2. 文件管理
- 同时参与检索的文件上限为 20 个
- 重复文件按指纹去重
- 删除文件时同步删除 IndexedDB 缓存

## 3. 结果展示
- 结果按文件和 Sheet 分组
- 只有命中的 Sheet 会被渲染
- 整体无结果时只显示一次统一空状态
- 支持命中 Sheet 快速跳转

## 4. 表头与列控制
- 每个 Sheet 可独立设置表头层数
- 表头最上方显示 Excel 列字母
- 支持 `全部列 / 仅标签列`
- 支持按某一层表头标签筛列
- 同一时间只允许一个表头层级的标签筛选生效

## 5. 合并单元格
- 解析 `!merges`
- 正确处理横向合并、纵向合并、区块合并
- 命中行中的合并区域需要显示根单元格内容

## 6. 交互细节
- 点击单元格复制当前单元格
- 点击行号复制整行
- 显示即时复制提示
- 提供返回顶部按钮
