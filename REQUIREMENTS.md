# 功能需求规格说明书 (REQUIREMENTS)

## 1. 搜索逻辑
- 仅允许严格子串匹配，不做模糊检索
- 输入过程中提供候选词
- 点击候选词或按 `Enter` 触发搜索

## 2. 文件管理
- 同时参与检索的文件上限为 1000 个
- 重复文件按指纹去重
- 文件导入必须保留绝对路径
- 支持按文件导入与按文件夹递归批量导入
- 删除文件只影响当前会话或当前配置，不修改原始 Excel
- 删除文件夹时应批量移除该文件夹节点下的全部已导入文件，且不修改原始 Excel
- 支持从结果和文件列表打开系统文件夹定位

## 2.1 配置导入导出
- 使用软件本体目录下 `config/` 目录中的 JSON 档案保存工作簿映射关系
- JSON 档案保留项目索引、原始文件链接与界面偏好；Excel 结构化缓存统一保存到软件目录下的数据库中
- 支持用户自主选择配置文件导入位置与导出位置
- UI 中必须可见当前配置，且不得要求用户先输入“建议文件名”
- 语言切换器必须作为全局界面语言入口，放在比配置区更显眼的位置
- 软件启动时默认优先读取“默认缓存”而非要求用户每次手动导入档案
- 手动导入任意配置后，当前工作集必须自动覆盖默认缓存
- 默认缓存与手动配置都必须从同一个 `config/` 目录读取和写入
- 默认缓存的用途是“打开即用”，手动配置的用途是“备份、分享、迁移当前工作场景”

## 3. 结果展示
- 结果按文件和 Sheet 分组
- 只有命中的 Sheet 会被渲染
- 整体无结果时只显示一次统一空状态
- 支持命中 Sheet 快速跳转
- 结果区需要使用虚拟滚动避免大量命中时卡顿
- 每个命中的 Sheet 标题区域必须提供直观的“打开所在位置”入口

## 4. 表头与列控制
- 每个 Sheet 可独立设置表头层数
- 表头最上方显示 Excel 列字母
- 支持 `全部列 / 仅标签列`
- 支持按某一层表头标签筛列
- 同一时间只允许一个表头层级的标签筛选生效

## 5. 合并单元格
- 解析 `!merges`
- 正确处理横向合并、纵向合并、区块合并
- 命中行中的合并区域需要显示根单元格内容

## 6. 交互细节
- 点击单元格复制当前单元格
- 点击行号复制整行
- 显示即时复制提示
- 提供返回顶部按钮
- 保持 `zh / en / ja` 三语支持可用
- 文件列表中的快捷操作使用“打开所在位置”，不再使用“收藏”
- 若当前模式无法获得真实绝对路径，仍需显示该操作，但以禁用态明确告知不可用
- 文件管理区应尽量精简，只保留文件名与所属文件夹等关键信息
- 文件管理区应以树状结构标记已导入的文件夹与文件

## 7. 性能约束
- 搜索逻辑必须保持严格 `String.includes()` 语义不变
- 大批量搜索应在 Web Worker 中执行，避免阻塞主线程

## 8. Windows 测试交付
- Windows 散装版与正式安装包的未来迭代，统一以 `github/Excel Strict Searcher-1.0.0-windows-setup.zip` 所体现的桌面应用逻辑为准
- 目标行为应是直接进入 Windows 应用窗口，而不是浏览器优先
- 当前主验证路径应以桌面窗口散包为准
- `windows-lightweight.zip` 可继续保留在 GitHub 发布目录中，但只作为兼容与对照用途
