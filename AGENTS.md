# AGENTS

This repository uses `AgentLogic/` as its external logic package.

Before structural edits:

1. Read `README.md`
2. Read `PROJECT_FACT_MAP.md`
3. Read `AgentLogic/01_MasterLogic.md`
4. Confirm `AgentLogic/15_LogicEntryChecklist.md`
5. Run `npm run check:logic` when logic-entry enforcement needs validation

Project rules:

- Keep search semantics strictly compatible with `String.includes()`
- Keep `zh-CN / en-US / ja-JP` available
- Keep the Windows primary direction desktop-window-first
- Update project docs when behavior or structure changes
