# EaaSE v1.1.0 功能增强规划方案 (Architecture Upgrade Plan)

本方案旨在将 EaaSE 从一个“纯网页工具”升级为“本地增强型工作站”，重点突破文件数量限制、实现本地路径联动以及构建可移植的本地配置文件体系。

---

## 1. 核心目标 (Core Objectives)
1. **容量飞跃：** 支持 100 个 Excel 文件并行检索，并保持 UI 流畅。
2. **深度联动：** 实现从搜索结果直接“一键打开”本地磁盘对应的 Excel 文件及其所在文件夹。
3. **档案化管理：** 废弃浏览器不稳定的 IndexedDB 缓存，转为可物理备份、可一键读取的本地 JSON 配置文件。

---

## 2. 技术架构演进 (Technical Evolution)
*   **当前架构：** 纯 React + Browser IndexedDB (受沙箱限制，无绝对路径，文件上限受内存制约)。
*   **目标架构：** **Hybrid (混合) 模式**。
    *   **Frontend (React):** 负责搜索逻辑、虚拟列表渲染、UI 交互。
    *   **Backend (Node.js/Lightweight Runtime):** 负责文件系统 I/O、调用 OS 指令、管理物理路径映射。
    *   **Bridge (IPC):** 通过本地 API 接口实现前后端数据同步。

---

## 3. 功能规划细节 (Functional Specifications)

### 3.1 突破 100 个文件限制 (High Capacity Support)
*   **内存管理策略：**
    *   **元数据先行 (Metadata First):** 启动时仅加载文件名、路径和指纹。只有在用户执行搜索或点击该文件时，才按需加载解析后的单元格数据。
    *   **虚拟滚动 (Virtualization):** 结果区域必须引入虚拟列表（如 `react-window` 思想），确保 100 个文件产生的数万行结果不会导致 DOM 崩溃。
    *   **多线程搜索:** 将 `String.includes` 的遍历逻辑移入 **Web Workers**，避免大批量文件匹配时阻塞主线程 UI。
*   **UI 增强：**
    *   侧边栏增加“文件筛选器”，支持在 100 个载入项中快速定位某个 Workbook。
    *   文件管理区使用树状结构展示已导入的文件夹与文件，便于确认批量导入范围。

### 3.2 绝对路径联动 (Local File Integration)
*   **导入逻辑重构：**
    *   新增“后端文件拾取器”：点击导入时，前端调用后端 API，后端唤起系统原生文件对话框，直接获取文件的 **绝对路径 (Absolute Path)**。
    *   新增“文件夹导入”：允许选择一个根文件夹并递归扫描其中的子文件夹与 Excel 文件。
*   **“打开位置”执行流：**
    1.  用户点击 `[打开文件夹]` 图标。
    2.  前端将配置文件中记录的 `absolutePath` 传给 Node.js 接口。
    3.  后端根据 OS 类型执行指令：
        *   **Windows:** `explorer.exe /select, "C:\path\to\excel.xlsx"` (自动选中文件并打开文件夹)。
        *   **macOS:** `open -R "/path/to/excel.xlsx"`。
*   **文件失效校验：** 启动时后台异步检查物理文件是否被移动或删除，若失效则在 UI 上标记。

### 3.3 本地化配置文件与一键读取 (File-based Configuration)
*   **存储位置：** 默认存储在软件本体目录下的 `config/` 文件夹中；读取与写入统一指向这一位置。
*   **配置文件 Schema (`config.json`):**
    ```json
    {
      "configName": "2024年财务审计项目",
      "lastUpdated": "2026-04-01",
      "workbooks": [
        {
          "fingerprint": "hash_xyz",
          "absolutePath": "D:/Project/Audit/data_01.xlsx",
          "headerDepth": 2,
          "isFavorite": true
        }
      ],
      "preferences": { "layout": "expanded", "theme": "light" }
    }
    ```
*   **一键切换场景：**
    *   用户可以创建多个 `.json` 档案（如 `ProjectA.json`, `ProjectB.json`）。
    *   提供“导入配置 / 导出配置”功能：文件路径由用户通过系统对话框自主选择。
    *   程序始终维护一份“默认缓存档案”用于打开即用；用户手动导入任意配置后，当前工作集会同步覆盖默认缓存。
*   **用途区分：**
    *   **默认缓存档案：** 用于恢复上次工作现场，强调“打开即用”。
    *   **项目档案：** 用于按项目保存、切换、备份、分享不同工作场景。
*   **便携性：** 只要 Excel 物理位置没变，用户拷贝 `config.json` 到另一台安装了 EaaSE 的电脑即可直接启动，无需重新导入。

### 3.4 网页快速模式回退 (Web Quickstart Fallback)
*   当本地 Node.js API 不可用时，前端允许退回浏览器文件导入，以便快速验证搜索与展示逻辑。
*   在该模式下，系统无法获得真实绝对路径，因此“打开所在位置”应保留为禁用态提示，而不是直接消失。

### 3.5 Windows 测试打包策略 (Windows Test Packaging Strategy)
*   Windows 散装测试包与正式安装包的长期目标，统一以 `github/Excel Strict Searcher-1.0.0-windows-setup.zip` 所代表的**桌面应用逻辑**为准。
*   该逻辑的关键要求是：
    1. 用户双击后直接进入 Windows 应用窗口。
    2. 产品体验以桌面应用为主，而不是浏览器优先。
    3. 本地路径联动、配置读写、打开所在位置等能力在桌面环境中完整可用。
*   当前已确认的 Windows 散装封装逻辑必须是：
    1. `EaaSE.exe` 作为启动入口。
    2. 启动本地 Node.js 服务。
    3. 使用 WebView2 在桌面窗口中承载 React 界面。
    4. 默认缓存与手动配置统一读写软件目录下的 `config/`。
    5. 缺失 Node.js 时，提示安装随包附带的官方 MSI。
    6. 缺失 WebView2 Runtime 时，提示安装随包附带的官方引导程序。
*   后续版本迭代时，Windows 散装版与正式安装包都必须沿用这条桌面窗口封装逻辑，不得再次分叉为“一个是桌面窗口、一个是浏览器启动器”。

---

## 4. 实施阶段规划 (Roadmap)

### 第一阶段：Node.js 后端接口强化
*   实现 `/api/fs/pick-files`: 调用原生对话框获取绝对路径。
*   实现 `/api/fs/open-explorer`: 封装系统指令用于定位文件。
*   实现 `/api/config/save` & `/api/config/load`: 读写本地磁盘 JSON 档案。

### 第二阶段：前端存储层重构
*   修改 `useWorkbooks` Hook，将其数据源从 `indexedDb.ts` 切换为后端 API。
*   在 `App.tsx` 中增加配置导入导出界面，并将语言切换器提升为全局入口。

### 第三阶段：性能优化
*   实现搜索逻辑的 Web Worker 并行化。
*   优化 `buildSectionRowCells` 算法，支持 100 文件量级的内存回收。

---

## 5. 风险评估 (Risk Assessment)
*   **路径权限：** 在某些严格权限的 Windows 目录下，Node.js 可能无法读取文件。**对策：** 在导入时进行 Read-Access 预检。
*   **文件同步性：** 用户在外部修改 Excel 后，本地缓存可能过期。**对策：** 维持指纹校验逻辑，若 `lastModified` 变动则自动后台静默重解析。

---
*Generated by Gemini CLI & Reviewed by Architect.*
