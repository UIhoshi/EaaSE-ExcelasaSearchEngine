# UI/UX 视觉与布局规范 (UI_UX_SPEC)

## 1. 页面结构
- 顶部为两列布局
- 左侧 `hero-card` 放标题、搜索、导入文件、导入文件夹、候选词、命中 Sheet 导航
- 右侧 `hero-actions` 放全局界面语言、当前布局、列模式、当前配置、关键词摘要
- 主内容区左侧是配置导入导出、文件管理和检索状态，右侧是结果区
- 配置区只保留“当前配置、导入配置、导出配置”三类信息，不显示建议文件名、不内嵌语言切换
- 文件管理区应采用精简树状信息展示，优先显示文件名与所属文件夹，超出宽度时使用省略号
- 文件夹导入后，树状结构需要清晰标记被导入的文件夹层级和文件叶子节点
- 文件夹节点必须提供删除入口，用于批量移除该节点下的全部已导入文件

## 2. 结果区
- 结果工具条放 `标准 / 扩展` 和 `全部列 / 仅标签列`
- 每个命中的 Sheet 使用独立 `sheet-box`
- `sheet-box` 顶部展示文件名、Sheet 名、命中行数、表头层数控件
- `sheet-box` 标题左侧提供直观的“打开所在位置”入口，便于快速定位具体文件
- 标签列筛选区位于表格上方
- 表格顶部固定多层表头和 Excel 列字母
- 行号列左侧固定

## 3. 布局模式
- `标准` 模式优先跟随屏幕宽度
- `扩展` 模式在存在真实搜索结果时允许宽表扩展
- 顶部摘要区和左侧栏宽度与下方布局保持一致

## 4. 交互反馈
- 关键词命中使用高亮标记
- 复制时在点击位置显示气泡
- 右下角显示返回顶部按钮
- 搜索区下方显示命中 Sheet 快捷跳转按钮
- 文件列表操作区提供“打开所在位置”
- 在网页快速模式下，如果文件没有真实绝对路径，“打开所在位置”必须显示为禁用态并给出说明
