# UI/UX 视觉与布局规范 (UI_UX_SPEC)

## 1. 页面结构
- 顶部为两列布局
- 左侧 `hero-card` 放标题、搜索、导入、候选词、命中 Sheet 导航
- 右侧 `hero-actions` 放当前布局、列模式、已载入文件数、关键词摘要
- 主内容区左侧是文件管理和检索状态，右侧是结果区

## 2. 结果区
- 结果工具条放 `标准 / 扩展` 和 `全部列 / 仅标签列`
- 每个命中的 Sheet 使用独立 `sheet-box`
- `sheet-box` 顶部展示文件名、Sheet 名、命中行数、表头层数控件
- 标签列筛选区位于表格上方
- 表格顶部固定多层表头和 Excel 列字母
- 行号列左侧固定

## 3. 布局模式
- `标准` 模式优先跟随屏幕宽度
- `扩展` 模式在存在真实搜索结果时允许宽表扩展
- 顶部摘要区和左侧栏宽度与下方布局保持一致

## 4. 交互反馈
- 关键词命中使用高亮标记
- 复制时在点击位置显示气泡
- 右下角显示返回顶部按钮
- 搜索区下方显示命中 Sheet 快捷跳转按钮
